#GENERAL
spring:
  application:
    name: colaborador
  profiles:
    active: development

#CLOUD
  cloud:
    config:
      enabled: true
      uri: http://127.0.0.1:8888
      fail-fast: true
      username: user
      password: admin